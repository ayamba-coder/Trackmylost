<nav>
{{#side-menu-toggle class='checkbtn'}}
        <i class="sidebar icon"></i>
{{/side-menu-toggle}}
  <label class="logo"><i class="ui icon search"></i>{{t "logo"}}</label>
        <select  class="uix" name="option" {{on "change" (fn this.switchLang value='target.value')}}>
  	  	<option value='en-us' selected>English</option>
  	  	<option value='fr-fr' >French</option>
	    </select>
    <ul class="w3-navbar longNav">
    {{#each this.navItems as |page|}}
      <li>
      {{#link-to page.title}}
        {{page.title}}
      {{/link-to}}
      </li>
    {{/each}}
  </ul>
    {{#side-menu class='shortNav'}}
    <header class="navbar navbar-default">
       Enongene 
    <div class="navbar-header">
      <select  class="w3-select" name="option" {{on "change" (fn this.switchLang value='target.value')}}>
  	  	<option value='en-us' selected>English</option>
  	  	<option value='fr-fr' >French</option>
	    </select>
    </div>
  </header>
    <ul class="w3-navbar">
    {{#each this.navItems as |page|}}
      <li>{{#side-menu-link-to page.title }} <i class="ui icon {{page.icon}} inverted green"></i>{{page.title}} {{/side-menu-link-to}}</li>
    {{/each}}
  </ul>
  {{/side-menu}} 
    {{content-backdrop}}
</nav>